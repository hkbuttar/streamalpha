# Where the FastAPI backend (backend/main.py) is running. Defaults to
# http://localhost:8000 in src/api.ts if unset -- only needed here if the
# backend runs somewhere else.
VITE_API_BASE_URL=http://localhost:8000
