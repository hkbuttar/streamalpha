# Editable local dependency on alpha-signal-lab, for config.universe (the
# shared ticker universe) only -- see alpha-signal-lab/pyproject.toml for
# why its packaging is restricted to just the config/ package. Assumes
# alpha-signal-lab is checked out as a sibling directory. IMPORTANT: pip
# resolves this relative path against the CWD it's invoked from, not against
# this file's location -- run `pip install -r requirements.txt` from inside
# streamalpha/, not from elsewhere with a `-r streamalpha/requirements.txt`
# pointer, or the path won't resolve. Revisit this at deployment time: it
# won't resolve on a host that only clones streamalpha.
-e ../alpha-signal-lab

confluent-kafka>=2.5
alpaca-py>=0.30
river>=0.21
fastapi>=0.110
uvicorn>=0.29
sqlalchemy>=2.0
psycopg[binary]>=3.1
python-dotenv>=1.0
pytest>=8.0
ruff>=0.6
